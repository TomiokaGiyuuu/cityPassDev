# React + Vite + Django + OpenAI + SQlite

To start Front-end:

#npm install

#vite dev

To start Back-end:

$ python -m venv venv

$ source venv/bin/activate

$ python manage.py migrate

$ python manage.py runserver
